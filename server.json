{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "com.opsmill/infrahub-mcp",
  "description": "An MCP server connecting AI assistants to Infrahub for infrastructure data management.",
  "repository": {
    "url": "https://github.com/opsmill/infrahub-mcp",
    "source": "github"
  },
  "version": "1.0.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "infrahub-mcp",
      "version": "1.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "INFRAHUB_ADDRESS",
          "description": "URL of your Infrahub instance (e.g. https://infrahub.example.com)",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "INFRAHUB_API_TOKEN",
          "description": "Infrahub API token. Use this or INFRAHUB_USERNAME + INFRAHUB_PASSWORD.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "INFRAHUB_USERNAME",
          "description": "Infrahub username for basic-auth login. Requires INFRAHUB_PASSWORD.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "INFRAHUB_PASSWORD",
          "description": "Infrahub password for basic-auth login. Requires INFRAHUB_USERNAME.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "INFRAHUB_TIMEOUT",
          "description": "HTTP request timeout in seconds (default: 30).",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
